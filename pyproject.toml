[project]
name = "devrel-io"
version = "0.1.0"
description = "Python CLI tool for developer relations workflows"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.35.2",
    "requests>=2.32.5",
]

[project.scripts]
gsheet = "gsheet:main"
