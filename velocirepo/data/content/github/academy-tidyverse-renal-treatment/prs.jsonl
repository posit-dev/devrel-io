{"source":"github","project_id":"academy-tidyverse-renal-treatment","target":"rstudio/academy-tidyverse-renal-treatment","id":"pr/2","ref":2,"title":"Replace CDN links with GitHub Pages and modernize dictionary theme","description":"## Summary\n\nThe legacy `rsacdn.link` CDN is being retired. This repo now publishes via **GitHub Pages** (enabled on `main`, root). This PR repoints the data dictionary references to the Pages URL and removes the CDN-hosted styling from the dictionary source.\n\n## Changes\n\n- **Repoint data dictionary links (7 milestone files):** the `mdrd` dictionary now links to `https://rstudio.github.io/academy-tidyverse-renal-treatment/assets/mdrd_dictionary.html` instead of `rsacdn.link`.\n- **Modernize the dictionary source:** `assets/mdrd_dictionary.qmd` was R Markdown (`output: html_document` + `cerulean` theme + a `teachthis` CDN stylesheet). It now uses native Quarto `format: html` with the default **cosmo** theme and `embed-resources: true` (self-contained single file, no external CDN assets).\n- **Re-rendered** `mdrd_dictionary.html`.\n\n## Verification\n\n- `grep` confirms **no** `rsacdn`, `cerulean`, or `teachthis` references remain in any `.qmd` or `.html`.\n- Dictionary re-rendered self-contained (no `_files/` sidecar).\n- GitHub Pages enabled and building; live dictionary URL returns HTTP 200.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","published_at":"2026-07-15T23:10:50Z","updated_at":"2026-07-15T23:42:42Z","url":"https://github.com/rstudio/academy-tidyverse-renal-treatment/pull/2","type":"pr","extra":{"author":"brendanhcullen","merged_at":"2026-07-15T23:42:42Z","state":"MERGED"}}
{"source":"github","project_id":"academy-tidyverse-renal-treatment","target":"rstudio/academy-tidyverse-renal-treatment","id":"pr/1","ref":1,"title":"Remove broken factor-order exercise from data types milestone","description":"The \"factor orders\" exercise asked learners to reorder a factor that `fct_recode()` already orders correctly, because recoding preserves the order of the underlying codes. The target plot needed no fixing, which was confusing.\n\nThis removes the exercise and its now-orphaned solution image, and updates the section intro to match the remaining content.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","published_at":"2026-07-06T19:47:01Z","updated_at":"2026-07-06T20:45:52Z","url":"https://github.com/rstudio/academy-tidyverse-renal-treatment/pull/1","type":"pr","extra":{"author":"brendanhcullen","merged_at":"2026-07-06T20:45:52Z","state":"MERGED"}}
{"source":"github","project_id":"academy-tidyverse-renal-treatment","target":"rstudio/academy-tidyverse-renal-treatment","id":"pr/3","ref":3,"title":"Regenerate milestone01.png for current ggplot2 binning defaults","description":"## Problem\n\nThe milestone 1 solution image, `images/milestone01.png`, cannot be reproduced from `data/mdrd.csv` with a current ggplot2. Learners trying to recreate it found that no value of `bins` or `binwidth` in `geom_histogram()` would match the distribution shown, which is confusing when the whole task is \"recreate this plot.\"\n\n## The data was never wrong\n\nReading the bar heights straight out of the committed PNG:\n\n- the bars sum to **1434** — exactly the number of `dropout == 0` observations in `data/mdrd.csv`\n- the lone rightmost bar sits at the true `max(gfr)` of **58.44**\n- the bin width is **1.9331**, exactly `diff(range(gfr)) / 29`\n\nSo the figure was generated from this same data set with the same default `bins = 30`. The values are unchanged.\n\n## Cause: ggplot2 changed where default bins are anchored\n\nggplot2 4.0.0 reworked `bin_breaks_bins()` to anchor the default breaks at `boundary = x_range[1] - width/2`, centring the outer bins on the data minimum and maximum. Earlier versions anchored at `boundary = width/2` relative to zero.\n\nThe bin *width* is identical under both rules here. Only the *offset* moves — by 0.4469 — which shifts observations between adjacent bins and visibly changes the bar heights:\n\n| | first bin edge | leading bar heights |\n|---|---|---|\n| ggplot2 3.x (committed image) | 0.967 | 1, 5, 21, 41, 77, 120, 157, **195**, 172, 180 |\n| ggplot2 4.0.x (current) | 1.413 | 1, 9, 19, 57, 77, 134, 165, **198**, 164, 185 |\n\nBecause the anchor moved and the width did not, no `bins` or `binwidth` value can recover the old figure — which is exactly the reported symptom. Adding `boundary = width/2` to the current code reproduces the previous image's bar heights exactly, confirming the diagnosis.\n\n## Fix\n\nRegenerated the figure from `data/mdrd.csv` using the milestone's own solution code:\n\n```r\nmdrd |\u003e\n  filter(dropout == 0) |\u003e\n  ggplot(aes(x = gfr)) +\n  geom_histogram()\n```\n\nSaved at the original **1344x960** (7x5in @ 192dpi), so the panel and axes land on the same pixels and only the bars change.\n\n## Verification\n\n- Bar heights read back out of the new PNG match `ggplot_build()` on the solution code exactly, across all 23 non-empty bins.\n- `images/milestone02.png` needs **no** change and is untouched. Milestone 2 passes an explicit `binwidth = 0.1`, which takes a code path this ggplot2 change did not affect; confirmed it still reproduces pixel-for-pixel.\n\n## Note\n\nThe regenerated image reflects ggplot2 4.x defaults. Learners installing packages from CRAN per the README will be on 4.x, so this is correct for them; anyone still on ggplot2 3.x would see the older binning.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","published_at":"2026-09-10T23:25:23Z","updated_at":"2026-09-10T23:26:51Z","url":"https://github.com/rstudio/academy-tidyverse-renal-treatment/pull/3","type":"pr","extra":{"author":"brendanhcullen","merged_at":"2026-09-10T23:26:51Z","state":"MERGED"}}
{"source":"github","project_id":"academy-tidyverse-renal-treatment","target":"rstudio/academy-tidyverse-renal-treatment","id":"pr/5","ref":5,"title":"Inline ignore_attr instead of a .groups setup-chunk object","description":"Closes #4.\n\nMilestones 2, 3, and 4 defined `.groups \u003c- c(\"class\", \"groups\")` in the hidden `setup` chunk and then passed it to `waldo::compare()` as `ignore_attr = .groups`. Learners who go straight to the comparison chunk without running the setup chunk first get:\n\n```\nError: object '.groups' not found\n```\n\nwhich says nothing about what actually went wrong.\n\nThis applies @jmclawson's suggested fix: inline the value at the call site and delete the object definition.\n\n```diff\n #| label: setup\n #| include: false\n-.groups \u003c-  c(\"class\", \"groups\")\n \n # Load your packages here\n```\n\n```diff\n-waldo::compare(patient_summary, solution, tolerance = 1e-4, ignore_attr = .groups)\n+waldo::compare(patient_summary, solution, tolerance = 1e-4, ignore_attr = c(\"class\", \"groups\"))\n```\n\nThree files, six lines. The setup chunks now match `academy-tidyverse-auto-insurance/policies_03_quarto_wrangle.qmd` exactly, and the inline `ignore_attr` matches the convention already used in `auto-insurance`, `covid`, `solar-power`, and `credit-risk`.\n\n## Scope\n\nI swept all 12 public project repos under the [`posit-academy-apprenticeship`](https://github.com/topics/posit-academy-apprenticeship) topic (excluding the IDE-tutorial repos and `academy-shiny`). This repo is the **only** one using the `.groups` helper-object pattern — every other R project either inlines `ignore_attr`, omits it, or has its `waldo::compare()` calls commented out; the two Python projects use pandas `DataFrame.compare()`.\n\nTwo adjacent items are **not** in this PR, noted here so they aren't lost:\n\n- `rstudio/academy-tidyverse-renal-treatment-10wk` has the same pattern in 5 files. (In that repo `mdrd_07_quarto_types.qmd` defines `.groups` but never uses it, and `mdrd_08_quarto_analyze.qmd` defines it as `c(\"class\", \"groups\", \"names\")` — a different value under the same name — so it needs a per-file fix rather than a blind search-and-replace.)\n- The pattern originates upstream in `academyMilestones`, including `inst/templates/milestone_0{3,4,5,7,8}.Rmd`, which is what new projects are generated from. Since the public repos are hand-mirrored rather than synced, this fix will not propagate upstream on its own.\n\n## Verification\n\nRan each milestone's reference solution against its `data/milestone0N.csv` in a fresh R session (waldo 0.6.2, dplyr 1.2.1):\n\n- **Reproduced the bug and confirmed the fix** — skipping the `setup` chunk, the old line raises `object '.groups' not found`; the new line runs and reports no differences.\n- **Grading is unchanged** — correct answers report `✔ No differences` for all three milestones (including M03/M04, where the learner's grouped tibble is compared against a plain tibble read from CSV, i.e. `ignore_attr` is actually doing work). Perturbed answers still report differences: values off by 1% (past `tolerance = 1e-4`), a missing `group_by(kidney_stage)` in M03, and a forgotten `left_join(supp)` in M04.\n- `quarto render` succeeds on all three files.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n","published_at":"2026-09-21T20:33:33Z","updated_at":"2026-09-21T20:56:21Z","url":"https://github.com/rstudio/academy-tidyverse-renal-treatment/pull/5","type":"pr","extra":{"author":"brendanhcullen","merged_at":"2026-09-21T20:56:21Z","state":"MERGED"}}
